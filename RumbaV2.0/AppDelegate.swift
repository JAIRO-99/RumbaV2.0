//
//  AppDelegate.swift
//  RumbaV2.0
//
//  Created by Jairo Laurente Celis on 20/09/24.
//
/*
import SwiftUI
import GoogleSignIn

class AppDelegate: NSObject, UIApplicationDelegate {
    
    // Método para configurar Google Sign-In
    func application(_ application: UIApplication,
                     didFinishLaunchingWithOptions launchOptions: [UIApplication.LaunchOptionsKey: Any]?) -> Bool {
        // Inicializa Google Sign-In aquí
        GIDSignIn.sharedInstance.clientID = "TU_CLIENT_ID_DE_GOOGLE"  // Reemplaza con tu client ID
        return true
    }
    
    // Manejar el URL cuando Google devuelve el resultado de autenticación
    func application(_ app: UIApplication, open url: URL,
                     options: [UIApplication.OpenURLOptionsKey: Any] = [:]) -> Bool {
        return GIDSignIn.sharedInstance.handle(url)
    }
}

*/
